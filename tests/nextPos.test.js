import { Node, Graph } from "../scriptsFiles/node_tree";


describe('Next Positions', ()=> {

    test('2 positions possible', ()=> {
        const firstPosition = new Node(0,0);
        const chess = new Graph(firstPosition);
        
    })
})